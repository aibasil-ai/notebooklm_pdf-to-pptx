## 1. 解析與渲染流程調整

- [x] 1.1 移除 `web/src/worker.js` 中的 `getTextContent()` 與 `extractTextItems()` 相關邏輯
- [x] 1.2 以 `pdfjs-dist` 渲染每頁為單張影像資料（保留可調 `scale` 常數）
- [x] 1.3 在每頁處理完成後釋放中間 buffer，確保取消流程可即時停止

## 2. PPT 輸出改為全版影像

- [x] 2.1 改用 `slide.addImage()` 將頁面影像以全版尺寸加入投影片
- [x] 2.2 依第一頁尺寸設定自訂 layout，保持 PDF 頁面比例
- [x] 2.3 移除文字圖形疊加輸出，確認每頁僅有一張影像

## 3. 狀態與錯誤處理

- [x] 3.1 更新進度與 log 訊息，移除「解析文字」敘述
- [x] 3.2 OffscreenCanvas 不支援或渲染失敗時，回報明確錯誤並中止
- [x] 3.3 確認取消流程能停止後續頁面並回報已中止狀態

## 4. 驗證與手動測試

- [ ] 4.1 以掃描 PDF 驗證輸出為單張影像投影片且不可編輯
- [ ] 4.2 以多頁 PDF 驗證頁面順序與比例正確
- [ ] 4.3 以大檔 PDF 驗證記憶體與輸出時間可接受
